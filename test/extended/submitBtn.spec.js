import sel from '../../data/selectors';
import {name, gender, age, story} from '../../data/testData';
import inputValues4 from '../../helpers/methods';

describe('Age field suit', function () {

    before('Open App', function () {
        browser.url('');
    });

    describe('Placeholder', function () {

        it('TC-0130 Verify that Create button is present in the correct place', function () {

        });

        it('TC-0138 Verify that Create button becomes disabled when  all the required fields are filled BUT then user deletes Name.', function () {

        });

    });
});
